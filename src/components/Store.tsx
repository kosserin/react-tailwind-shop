import React from "react";
import Header from "./Header";

const Store = () => {
  return (
    <main>
      <Header></Header>
      <section>Store</section>
    </main>
  );
};

export default Store;
